# ---- IDA Pro 数据库/工作区 (大型二进制, 不入库) ----
*.i64
*.idb
*.id0
*.id1
*.id2
*.nam
*.til
*.i64.bak
*.idb.bak
# 根目录 193MB 的主分析库
/gamemd94.i64

# ---- 逆向过程中产生的各类分析库/探针库副本 ----
# 上面 *.i64 已覆盖，此处保留说明

# ---- 源文件手动备份(落地前快照), 不入库 ----
*.bak
*.bak_*
*.orig_*

# ---- YRpp: 独立第三方参考源码(自带 git 上游/子模块), 不并入本仓库 ----
/YRpp/

# ---- 编辑器/系统杂项 ----
.DS_Store
Thumbs.db
*.swp
*~

# ---- Agent 记忆/指令文件(★ 严禁提交到公开仓库) ----
# DSH 会自动加载工作区根的 AGENTS.md / CLAUDE.md 系列；它们只服务本地协作，
# 含内部进度、用户画像与未公开结论 —— 绝不允许进入公开仓库。
AGENTS.md
AGENTS.local.md
CLAUDE.md
CLAUDE.local.md
# Agent 记忆与工作资产目录(DSH 自有，非项目交付物)
.dsh/
# WorkBuddy 工作区记忆目录(含项目日志与用户画像，与 .dsh/ 同性质，严禁提交)
.workbuddy/
